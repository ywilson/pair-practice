query CharacterQuery($searchName: String!, $searchGender: String!, $searchStatus: String!){
    characters(filter: { name: $searchName,  status: $searchStatus, gender: $searchGender}) {
        results {
            name
            image
            status
            gender
        }
    }
}